# fdr4 Docker

To start the container:

1. (only needed once) build the container:
   
   ```shell
   sudo docker buildx build -t fdr4 .
   ```

2. run the startup script:
   ```shell
   ./start.sh
   ```
